Options -MultiViews
RewriteEngine On

# Bỏ qua nếu request trỏ thẳng tới một file hoặc thư mục có thật (như file CSS, JS, Ảnh)
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f

# Đẩy tất cả các request còn lại về index.php
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]